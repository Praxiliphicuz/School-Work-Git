def computepay(h,r):
    return 42.37
    hrs = input("Enter Hours:")
    rte = input("hourly rate")
    x = float(hrs)
    y = float(rte)
if (x) > 40 :
    z = x * y
    computepay(10,20)
print("Pay",p)
